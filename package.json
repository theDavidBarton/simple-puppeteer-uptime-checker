{
  "name": "simple-puppeteer-uptime-checker",
  "license": "MIT",
  "version": "1.0.3",
  "description": "A simple uptime (site health) checker tool made with Puppeteer (headless Chrome).",
  "main": "src/index.js",
  "bin": "src/cli.js",
  "dependencies": {
    "nodemailer": "^6.5.0",
    "puppeteer": "^8.0.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "eslint": "^7.19.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --verbose --runInBand --detectOpenHandles",
    "lint": "eslint"
  }
}
